# Branch and attribution audit

Former repository name: `icml26-repro-WtINGEMjTB-topology-preserving-graph-coarsening`

Target repository name: `icml26-topology-preserving-graph-coarsening`

The repository currently exposes only `main`; the source clone contains no
former `orx/*` branches. Therefore there is no branch experiment lineage to
rename. The current `main` tip contains the contract, source pins, Claim 1 toy,
raw evidence, and the documentation in the root.

## Publication checklist

- [x] README documents the paper, all six claims, repository contents, Claim 1 evidence path, citation, and thank-you note.
- [x] Current branch state is explicitly documented as `main` only.
- [x] No former `orx/*` branch names are present in the cloned refs.
- [ ] Normalize all reachable commit author and committer identities.
- [ ] Rename the GitHub repository.
- [ ] Push the clean name and verify the live default branch, README, refs, and identity set.

## Attribution policy

All reachable commits will be normalized to:

```text
MachineLearning-Nerd <37579156+MachineLearning-Nerd@users.noreply.github.com>
```
